"""Áudio do sistema (loopback). Captura o que toca nos alto-falantes — YouTube,
Spotify, chamadas, qualquer som do Windows — via WASAPI loopback, sem usar o
microfone. Transcreve com o mesmo `ISTT` da voz do usuário."""
